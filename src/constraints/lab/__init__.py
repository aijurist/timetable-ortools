"""Lab constraint bundle.

Modules inside this package enforce lab coverage, room allocation, capacity
analysis, shift logic, and specialised core-lab handling. Each module should
export concrete :class:`~src.constraints.base.Constraint` implementations and
register them with :mod:`src.constraints.registry`.
"""
